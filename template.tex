\documentclass{article}
\usepackage{changepage}
\usepackage{CJKutf8}
\usepackage{geometry}
 \geometry{
 a4paper,
 total={170mm,257mm},
 left=30mm,
 top=30mm,
 }

\begin{document}
\begin{CJK}{UTF8}{min}
  
\section*{Mini Wörterbuch}

{% for entry in entries %}
\textbf{ {{-entry.name}}} {{entry.hiragana}} \textit{ {{-entry.type}}} {{entry.translation}} {{ "\\textbf{" ~ entry.chapter ~ "}" if entry.chapter }}
        {% if entry.get('kanji') or entry.get('code') or entry.get('misc') %}
                \begin{adjustwidth}{0.5cm}{}
                {% if entry.get('kanji') or entry.get('code') %}
                        \textbf{Kanji:} {{ entry.kanji }} \textbf{Code:} {{ entry.code }}
                        {{ "\\" if entry.get('misc') }}
                {% endif %}
                {{ "\\textbf{Info:} " ~ entry.misc if entry.get('misc') }}
                \end{adjustwidth}
        {% else %}
                \\
        {% endif %}
{% endfor %}

\end{CJK}
\end{document}
