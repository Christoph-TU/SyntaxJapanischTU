\documentclass{article}
\usepackage{CJKutf8}
\usepackage{geometry}
 \geometry{
 a4paper,
 total={170mm,257mm},
 left=30mm,
 top=30mm,
 }

\begin{document}
\begin{CJK}{UTF8}{min}
  
\section*{Mini Wörterbuch}

{% for entry in entries %}
\noindent \textbf{ {{-entry.name}}} {{entry.hiragana}} \textit{ {{-entry.type}}} {{entry.translation}} 
        {% if entry.chapter %}
        \textbf{ {{-entry.chapter}}} 
        {% endif %}
        {% if entry.get('kanji') or entry.get('code') %}
        \\
        \hspace*{0.5cm}\textbf{Kanji:} {{ entry.kanji }} \textbf{Code:} {{ entry.code }}
        {% endif %}
        {% if entry.get('misc') %}
        \\
        \hspace*{0.5cm}\textbf{Info:} {{ entry.misc }}
        {% endif %}
        \\
{% endfor %}

\end{CJK}
\end{document}
