\documentclass{article}
\usepackage{CJKutf8}

\begin{document}
\begin{CJK}{UTF8}{min}

\section*{Dictionary}

\begin{itemize}
{% for entry in entries %}
  \item {{ entry.name }} ({{ entry.hiragana }}) \\
        \textbf{Wortart:} {{ entry.type }} \\
        {% if entry.get('kanji') %}
        \textbf{Kanji:} {{ entry.kanji }} \\
        {% endif %}
        {% if entry.get('code') %}
        \textbf{Code:} {{ entry.code }} \\
        {% endif %}
        \textbf{Übersetzung:} {{ entry.translation }}
        {% if entry.get('misc') %}
        \\
        \textbf{Bemerkung:} {{ entry.misc }}
        {% endif %}
{% endfor %}
\end{itemize}

\end{CJK}
\end{document}
